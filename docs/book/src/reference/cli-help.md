# `fcs` command line

<!-- Generated by scripts/docs/generate-reference.sh from `fcs --help`; do not edit. -->

Full help for the client and each subcommand, as printed by the binary.
The narrative guide is [Command-line client](../imported/cli.md).

## `fcs --help`

```text
Search the code index kept by a running fast_code_search server.

`fcs QUERY` is `fcs search QUERY`. The query syntax is the web UI's: several words must all appear in a file (lines with the phrase rank first), "quoted phrase", -term, file:PATTERN, -file:PATTERN, lang:rust, case:yes, word:yes.

Exit status: 0 = matches found, 1 = no matches, 2 = error.

Usage: fcs [OPTIONS] <COMMAND>

Commands:
  search   Search file contents (the default; `fcs QUERY` works too)
  refs     Find references (call sites, type mentions) of an identifier
  symbols  Search symbol definitions (functions, types, classes, ...) only
  status   Show whether the server is reachable and what it has indexed
  help     Print this message or the help of the given subcommand(s)

Options:
      --server <URL>
          Server base URL (default: $FCS_SERVER, then the config's web_address, then http://127.0.0.1:8080)

      --config <FILE>
          Configuration file (default: $FCS_CONFIG, ./fast_code_search.toml, ~/.config/fast_code_search/config.toml)

      --offline
          Search the on-disk index directly instead of asking the server

      --no-offline
          Never fall back to the on-disk index when the server is unreachable

      --index-path <FILE>
          Index file for offline searches (default: the config's index_path)

      --format <FORMAT>
          Output layout (default: grouped on a terminal, vimgrep when piped)

          Possible values:
          - grouped: File headings with `line:col: text` underneath (default on a terminal)
          - vimgrep: One `path:line:col:text` line per hit (default when piped)
          - json:    The server's JSON response, one object

      --json
          Shorthand for --format json

  -l, --files-with-matches
          Print only the files that match, one per line

      --absolute
          Print absolute paths (needs the roots from the configuration)

      --color <COLOR>
          Colour output: auto (terminal only, honours NO_COLOR), always, never
          
          [default: auto]
          [possible values: auto, always, never]

  -q, --quiet
          No summary or fallback notes on stderr

  -h, --help
          Print help (see a summary with '-h')

  -V, --version
          Print version

Examples:
  fcs 'fn main'                 lines holding both words, phrase first
  fcs -e 'fn\s+\w+\(' -g '*.rs'  regex, Rust files only
  fcs refs SearchEngine         call sites and type mentions
  fcs symbols parse_query       definitions only
  fcs -l TODO | xargs $EDITOR   files containing TODO
  vim -q <(fcs --format vimgrep TODO)
  fcs status                    is the server up, what does it hold
```

## `fcs search --help`

```text
Search file contents (the default; `fcs QUERY` works too)

Usage: fcs search [OPTIONS] <QUERY>...

Arguments:
  <QUERY>...
          The query (web UI syntax; quote it if it has spaces)

Options:
  -e, --regex
          Treat the query as a regular expression

  -s, --case-sensitive
          Match case exactly

  -i, --ignore-case
          Ignore case (the default for plain text; useful to override case:yes)

  -w, --word
          Match whole words only

  -n, --max <N>
          Maximum hits to print (1-1000)
          
          [default: 50]

      --server <URL>
          Server base URL (default: $FCS_SERVER, then the config's web_address, then http://127.0.0.1:8080)

      --config <FILE>
          Configuration file (default: $FCS_CONFIG, ./fast_code_search.toml, ~/.config/fast_code_search/config.toml)

      --offset <N>
          Skip the first N hits (paging; ordering is deterministic)
          
          [default: 0]

  -C, --context <N>
          Lines of context before and after each hit (0-10)
          
          [default: 0]

      --offline
          Search the on-disk index directly instead of asking the server

  -g, --glob <GLOB>
          Only files matching this glob (repeatable; `src/**/*.rs`, `*.py`, `tests`)

      --no-offline
          Never fall back to the on-disk index when the server is unreachable

      --exclude <GLOB>
          Skip files matching this glob (repeatable)

      --index-path <FILE>
          Index file for offline searches (default: the config's index_path)

      --format <FORMAT>
          Output layout (default: grouped on a terminal, vimgrep when piped)

          Possible values:
          - grouped: File headings with `line:col: text` underneath (default on a terminal)
          - vimgrep: One `path:line:col:text` line per hit (default when piped)
          - json:    The server's JSON response, one object

      --rank <RANK>
          Ranking: auto, fast (metadata only, big candidate sets), full
          
          [default: auto]
          [possible values: auto, fast, full]

      --json
          Shorthand for --format json

      --timeout-ms <MS>
          Stop scanning after this many milliseconds and return the best so far
          
          [default: 0]

  -l, --files-with-matches
          Print only the files that match, one per line

      --absolute
          Print absolute paths (needs the roots from the configuration)

      --color <COLOR>
          Colour output: auto (terminal only, honours NO_COLOR), always, never
          
          [default: auto]
          [possible values: auto, always, never]

  -q, --quiet
          No summary or fallback notes on stderr

  -h, --help
          Print help (see a summary with '-h')
```

## `fcs refs --help`

```text
Find references (call sites, type mentions) of an identifier

Usage: fcs refs [OPTIONS] <QUERY>...

Arguments:
  <QUERY>...
          The query (web UI syntax; quote it if it has spaces)

Options:
  -e, --regex
          Treat the query as a regular expression

  -s, --case-sensitive
          Match case exactly

  -i, --ignore-case
          Ignore case (the default for plain text; useful to override case:yes)

  -w, --word
          Match whole words only

  -n, --max <N>
          Maximum hits to print (1-1000)
          
          [default: 50]

      --server <URL>
          Server base URL (default: $FCS_SERVER, then the config's web_address, then http://127.0.0.1:8080)

      --config <FILE>
          Configuration file (default: $FCS_CONFIG, ./fast_code_search.toml, ~/.config/fast_code_search/config.toml)

      --offset <N>
          Skip the first N hits (paging; ordering is deterministic)
          
          [default: 0]

  -C, --context <N>
          Lines of context before and after each hit (0-10)
          
          [default: 0]

      --offline
          Search the on-disk index directly instead of asking the server

  -g, --glob <GLOB>
          Only files matching this glob (repeatable; `src/**/*.rs`, `*.py`, `tests`)

      --no-offline
          Never fall back to the on-disk index when the server is unreachable

      --exclude <GLOB>
          Skip files matching this glob (repeatable)

      --index-path <FILE>
          Index file for offline searches (default: the config's index_path)

      --format <FORMAT>
          Output layout (default: grouped on a terminal, vimgrep when piped)

          Possible values:
          - grouped: File headings with `line:col: text` underneath (default on a terminal)
          - vimgrep: One `path:line:col:text` line per hit (default when piped)
          - json:    The server's JSON response, one object

      --rank <RANK>
          Ranking: auto, fast (metadata only, big candidate sets), full
          
          [default: auto]
          [possible values: auto, fast, full]

      --json
          Shorthand for --format json

      --timeout-ms <MS>
          Stop scanning after this many milliseconds and return the best so far
          
          [default: 0]

  -l, --files-with-matches
          Print only the files that match, one per line

      --absolute
          Print absolute paths (needs the roots from the configuration)

      --color <COLOR>
          Colour output: auto (terminal only, honours NO_COLOR), always, never
          
          [default: auto]
          [possible values: auto, always, never]

  -q, --quiet
          No summary or fallback notes on stderr

  -h, --help
          Print help (see a summary with '-h')
```

## `fcs symbols --help`

```text
Search symbol definitions (functions, types, classes, ...) only

Usage: fcs symbols [OPTIONS] <QUERY>...

Arguments:
  <QUERY>...
          The query (web UI syntax; quote it if it has spaces)

Options:
  -e, --regex
          Treat the query as a regular expression

  -s, --case-sensitive
          Match case exactly

  -i, --ignore-case
          Ignore case (the default for plain text; useful to override case:yes)

  -w, --word
          Match whole words only

  -n, --max <N>
          Maximum hits to print (1-1000)
          
          [default: 50]

      --server <URL>
          Server base URL (default: $FCS_SERVER, then the config's web_address, then http://127.0.0.1:8080)

      --config <FILE>
          Configuration file (default: $FCS_CONFIG, ./fast_code_search.toml, ~/.config/fast_code_search/config.toml)

      --offset <N>
          Skip the first N hits (paging; ordering is deterministic)
          
          [default: 0]

  -C, --context <N>
          Lines of context before and after each hit (0-10)
          
          [default: 0]

      --offline
          Search the on-disk index directly instead of asking the server

  -g, --glob <GLOB>
          Only files matching this glob (repeatable; `src/**/*.rs`, `*.py`, `tests`)

      --no-offline
          Never fall back to the on-disk index when the server is unreachable

      --exclude <GLOB>
          Skip files matching this glob (repeatable)

      --index-path <FILE>
          Index file for offline searches (default: the config's index_path)

      --format <FORMAT>
          Output layout (default: grouped on a terminal, vimgrep when piped)

          Possible values:
          - grouped: File headings with `line:col: text` underneath (default on a terminal)
          - vimgrep: One `path:line:col:text` line per hit (default when piped)
          - json:    The server's JSON response, one object

      --rank <RANK>
          Ranking: auto, fast (metadata only, big candidate sets), full
          
          [default: auto]
          [possible values: auto, fast, full]

      --json
          Shorthand for --format json

      --timeout-ms <MS>
          Stop scanning after this many milliseconds and return the best so far
          
          [default: 0]

  -l, --files-with-matches
          Print only the files that match, one per line

      --absolute
          Print absolute paths (needs the roots from the configuration)

      --color <COLOR>
          Colour output: auto (terminal only, honours NO_COLOR), always, never
          
          [default: auto]
          [possible values: auto, always, never]

  -q, --quiet
          No summary or fallback notes on stderr

  -h, --help
          Print help (see a summary with '-h')
```

## `fcs status --help`

```text
Show whether the server is reachable and what it has indexed

Usage: fcs status [OPTIONS]

Options:
      --server <URL>
          Server base URL (default: $FCS_SERVER, then the config's web_address, then http://127.0.0.1:8080)

      --config <FILE>
          Configuration file (default: $FCS_CONFIG, ./fast_code_search.toml, ~/.config/fast_code_search/config.toml)

      --offline
          Search the on-disk index directly instead of asking the server

      --no-offline
          Never fall back to the on-disk index when the server is unreachable

      --index-path <FILE>
          Index file for offline searches (default: the config's index_path)

      --format <FORMAT>
          Output layout (default: grouped on a terminal, vimgrep when piped)

          Possible values:
          - grouped: File headings with `line:col: text` underneath (default on a terminal)
          - vimgrep: One `path:line:col:text` line per hit (default when piped)
          - json:    The server's JSON response, one object

      --json
          Shorthand for --format json

  -l, --files-with-matches
          Print only the files that match, one per line

      --absolute
          Print absolute paths (needs the roots from the configuration)

      --color <COLOR>
          Colour output: auto (terminal only, honours NO_COLOR), always, never
          
          [default: auto]
          [possible values: auto, always, never]

  -q, --quiet
          No summary or fallback notes on stderr

  -h, --help
          Print help (see a summary with '-h')
```

